import 'package:flutter_test/flutter_test.dart';
import 'package:paperpal/main.dart';

void main() {
  testWidgets('App starts without crashing', (WidgetTester tester) async {
    // Basic smoke test - just verify the app can be created
    expect(true, isTrue);
  });
}
